{
  "name": "playwrightproject",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "smoke": "playwright test --grep @smoke",
    "regression": "playwright test --grep @regression",
    "test:allure": "playwright test --reporter=list,json,allure-playwright",
    "test:single:allure": "playwright test tests/launchbrowser.spec.js --reporter=list,json,allure-playwright && allure generate allure-results --clean -o allure-report && allure open allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.0",
    "allure-commandline": "^2.34.1",
    "allure-playwright": "^3.4.1"
  },
  "dependencies": {
    "@cucumber/cucumber": "^12.2.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
